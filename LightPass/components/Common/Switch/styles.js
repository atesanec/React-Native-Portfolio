
import { StyleSheet } from 'react-native';
import { spacing } from '../../../common/theme';

const { xs } = spacing.spacingValues;

export default StyleSheet.create({
  switch: {
    marginLeft: -xs,
  },
});

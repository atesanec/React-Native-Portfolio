import { defineMessages } from 'react-intl';

export default defineMessages({
  appBar: {
    id: 'User.LoginWorkspaceScreen.appBar',
    defaultMessage: 'Login to "Digital Access Control"',
  },
});
